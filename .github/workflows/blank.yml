name: Update README

on:
  schedule:
    # Menjalankan workflow setiap 5 menit
    - cron: "*/5 * * * *"

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
    # Checkout repositori
    - uses: actions/checkout@v4

    # Update README.md dengan data baru
    - name: Update README
      run: |
        echo '{
          "name": "Yogawan Aditya Pratama",
          "education": "University of Technology Yogyakarta",
          "interest": ["Bitcoin", "JKT48"],
          "skills": {
            "Graphic Designer": ["Figma", "CorelDraw"],
            "UI Designer": ["Figma", "Sketch"],
            "Front-End": ["Saas", "Tailwind (On-Going)", "ReactJS", "NextJS (On-Going)"],
            "Back-End": ["MonggoDB", "ExpressJS", "MySQL"]
          },
          "updated_at": "'$(date -u)'"
        }' > README.md

    # Commit dan push perubahan, dengan penanganan error
    - name: Commit and push changes
      run: |
        git config --global user.name "github-actions[bot]" || exit 1
        git config --global user.email "github-actions[bot]@users.noreply.github.com" || exit 2
        git add README.md || exit 3
        git commit -m "Update README automatically" || exit 4
        git push || exit 5
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
